#include <iostream>
using namespace std;
int main() {
    int a, b;
    int x, y, z, q, w;
    cin >> a >> b;
    cin >> x >> y >> z >> q >> w;
    cout << x-a*b << " " << y-a*b << " " << z-a*b << " " << q-a*b << " " << w-a*b;
}