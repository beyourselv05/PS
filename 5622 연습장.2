//1i. 할머니가 외운 단어가 주어짐.
//1o. 다이얼을 걸기위한 최소시간.

//alp 배열에 순서대로 3,3,3,4,4,4,5,5,5, 처럼 요소를 넣음.
//

#include <stdio.h>
#include <string.h>

int main() {
    int count=0;
    int len;
    char str[16] = { 0, };
    int alp[27] = {3,3,3,4,4,4,5,5,5,6,6,6,7,7,7,8,8,8,8,9,9,9,10,10,10,10};
    int cmp;
    
    scanf("%s", str);    //string = WA
    len = strlen(str);   //len = 2
    
    //str배열에 있는 AD 숫자로 변환하기
    for(int i=0; i<len; i++) {
        str[i] = str[i] - 65; 
        printf("%d ", str[i]);     //str a0 b1 c2 d3
    }
#if 1    
    for(int i=0; i<len; i++) {
        for(int j=0; j<26; j++) {
            if (str[i]+3 == alp[j]) count += alp[j];
        }
    }
    printf("%d", count);
#endif    
}

//abc 3, def 4, ghi 5, jkl 6, mno 7, pqrs 8, tuv 9, wxyz 10sec
